import 'package:stacked/stacked.dart';

class StockWarningScreenViewModel extends FutureViewModel {
  @override
  Future futureToRun() async {
    try {} catch (e) {
      throw UnimplementedError();
    }
  }
}
