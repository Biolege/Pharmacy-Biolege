import 'package:stacked/stacked.dart';

class OrderConfirmationViewModel extends FutureViewModel {
  @override
  Future futureToRun() async {
    try {} catch (e) {
      throw UnimplementedError();
    }
  }
}
