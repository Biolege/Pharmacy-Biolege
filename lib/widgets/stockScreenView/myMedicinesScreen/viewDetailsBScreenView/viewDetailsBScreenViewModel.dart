import 'package:stacked/stacked.dart';

class ViewDetailsBScreenViewModel extends FutureViewModel {
  @override
  Future futureToRun() async {
    try {} catch (e) {
      throw UnimplementedError();
    }
  }
}
