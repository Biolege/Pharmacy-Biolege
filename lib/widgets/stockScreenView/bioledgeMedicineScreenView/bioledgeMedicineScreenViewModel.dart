import 'package:stacked/stacked.dart';

class BioledgeMedicineScreenViewModel extends FutureViewModel {
  @override
  Future futureToRun() async {
    try {} catch (e) {
      throw UnimplementedError();
    }
  }
}
